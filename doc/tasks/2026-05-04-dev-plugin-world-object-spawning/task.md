# Task

Fix and expand the dev plugin world-object spawning workflow so spawned objects are created authoritatively by the server and behave like generated world objects: replicated to clients and included in map persistence.

Add an interactive placement-selection flow where the client previews the target location under the mouse before sending a placement request.
