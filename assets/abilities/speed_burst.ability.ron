#![enable(implicit_some)]
(
    startup_ticks: 1,
    active_ticks: 20,
    recovery_ticks: 6,
    cooldown_ticks: 60,
    effects: [
        OnTick(effect: Buff(stat: "speed", multiplier: 2.5, duration_ticks: 60, target: Caster)),
        WhileActive(SetVelocity(speed: 20.0, target: Caster)),
    ],
)
